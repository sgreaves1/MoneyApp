version: '2'
services:
  api:
    image: node:carbon-alpine
    volumes:
      - .:/app
    working_dir: /app
    environment:
      - PORT=4000
    ports:
      - "4000:4000"
    command: ["npm", "start"]
  mongodb:
      image: mongo:3.6
      ports:
        - "27017:27017"
